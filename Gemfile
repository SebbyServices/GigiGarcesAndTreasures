source "https://rubygems.org"

# github-pages pins all Jekyll deps to match the Pages build environment.
# Local dev: bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-seo-tag"
end

# Required for Ruby 3+
gem "webrick", "~> 1.8"
